// For Add On First Element Use .Unshift && For Remove an First Element Use .Shift  

// For Add On Last Element Use .Push && For Remove an First Element Use .pop  


// var fruits = ["Banana", "Orange", "Apple", "Mango"];
// fruits.splice(1,1, "Pineapple");
// console.log(fruits);
function addNumber(number1, number2) {
    return "Result is " + number1 + number2;
   }
   
   var result = addNumber(5, 4);
   console.log(result);